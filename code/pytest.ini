[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*

markers =
    integration: marks tests as requiring live AWS (deselect with -m "not integration")
    slow: marks tests expected to take > 60 seconds

addopts = -v --tb=short
